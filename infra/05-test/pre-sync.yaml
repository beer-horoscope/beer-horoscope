apiVersion: v1
kind: Pod
metadata:
  labels:
    run: busybox
  name: presync
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
    argocd.argoproj.io/sync-wave: "0"
spec:
  containers:
  - args:
    - /bin/sh
    - -c
    - sleep 15; echo "hello world pre sync"
    image: busybox
    name: busybox
  dnsPolicy: ClusterFirst
  restartPolicy: Never
status: {}